# E03 — Migraciones reproducibles de PIN

Estado inicial: NOT_STARTED. Dependencias: E00; E01.

## Resultado

Eliminar deriva schema/historial sin esconderla con db push.

## Archivos y responsabilidad

packages/api/prisma/schema.prisma, schema.supabase.prisma, ambos historiales de migración y tests PG relacionados. Este alcance corresponde al ejecutor de esta ficha. Leer instrucciones aplicables; preservar cambios ajenos. Si se habilitan agentes en otra ejecución, asignar archivos explícitos y no revertir ediciones de otros.

## Trabajo acotado

Añadir migración aditiva compatible con nombres/mapeos reales de StaffUser y constraint Prisma. No usar literalmente VARCHAR(64) si schema define otro tipo sin mapearlo. Campos legados nullable y plan de duplicados explícito; no asignar huellas aleatorias como identidad. Versionar equivalentes de SQLite si ese historial se mantiene.

## Verificación obligatoria

S23: instalar historial desde cero y upgrade desde snapshot sintético previo; introspección y alta/login concurrentes en PG. Verificar paridad. No usar db push en el gate de migración.

## Criterio de cierre

Columna/índice creados por migraciones y pruebas pasan. No aplicar a Supabase real; SQL y rollback conservador revisables.

## Reporte y continuidad

Crear reportes/E03.md con SHA entrada/salida, diagnóstico confirmado o descartado, diff, pruebas/comandos/exit codes, evidencia y pendientes. Actualizar CONTINUAR.md. No emitir VERIFIED_LOCAL si sólo se escribió código o un test no corrió. No implementar otra ficha no habilitada; si una dependencia falla, volver a su causa.

