# Reporte de ejecución: E21 — Regresión de seguridad y recorrido completo

Fecha: 2026-09-16
Entrada: rama `codex/servicio-remediacion`, HEAD `7bcddf6bf298f6cb15da70579fb49b9ecd7d1c83`, con árbol sucio E00–E20 preservado (sin `reset/clean/checkout/switch/merge/rebase`, sin borrar ni revertir ediciones ajenas).
Salida: mismo SHA base, sin commit. Sólo archivos autorizados por el handoff E21.
Worktree: `C:/Users/rodri/Desktop/AI/Projects/mdpmesasvivas-servicio-remediacion`
Ejecutor: OpenCode con `opencode/muse-spark-1.3-contributor-free`. Sin cambio de modelo, sin otros agentes.
Revisión de implementación: autorrevisión estática de OpenCode; **no es
revisión independiente**. Verificación posterior: Codex ejecutó la focal, la
regresión completa, el build, la matriz y los checks de diff en forma separada.
Gate: `VERIFIED_LOCAL` técnico; no equivale a GO humano/cloud.

## Alcance y diagnóstico

E21 corrige cinco regresiones reales documentadas por la supervisión (baseline completo previo), sin reabrir E00–E20 ni implementar E22+:

1. **S07 / navegación (contrato 02 §2):** `App.tsx` exponía Cocina como tercera entrada primaria permanente (`grid-cols-3` + botón `selectTab('kitchen')` + `<span>Cocina</span>`), contradiciendo el contrato que reserva Cocina como acceso configurable dentro de Más. El test S07 heredado prohibía todo `selectTab('kitchen')`, volviéndose obsoleto tras E14. Confirmado por lectura de `App.tsx` vs `02-CONTRATO-SERVICIO.md` y reporte E20 (regresión 48/49 por esa expectativa).
2. **Polling/E08:** `polling-clients-reconnect.test.ts` exigía `PollingCoordinator` dentro de `useSSE.ts`, aunque E08 documenta ese archivo como adaptador y el dueño real es `useServiceSync.ts` (único `PollingCoordinator` + `start/stop/triggerNow`). Confirmado por lectura de ambos hooks.
3. **E10 labels:** `e10-table-detail-actions.test.ts` usaba `Mesa ${Math.floor(Math.random()*90+10)}` (rango 10..99) que colisiona con fixtures del mismo restaurante. Confirmado por lectura.
4. **Matriz de rutas:** el scanner no conocía `verifySettlementAuthorization` (clasificaba las dos rutas settle como `ANON` vs manifiesto `MANAGER`) y faltaban `POST /v1/staff/restaurants/:id/service-tasks/act` (E14, `verifyStaffToken`) y `POST /v1/staff/terminal/provision` (PIN validado dentro de la ruta, sin preHandler). Confirmado por lectura de `check-route-matrix.mjs`, `orders.routes.ts`, `service.routes.ts`, `staff.routes.ts` y `auth.middleware.ts`.
5. **Capacidades/E12:** `capabilities-unit.test.ts` esperaba 11 capacidades y fixture sin `allowWaitersToCollectCash`, aunque E12 implementó `waiter_cash_collection` (12.ª capacidad, `ConfigService.buildCapabilities` + shared). Confirmado por lectura de `config.service.ts` y `packages/shared/src/index.ts`.

Hipótesis descartadas: ninguna. Las cinco se confirmaron por lectura cruzada; no se forzó ningún resultado ni se maquilló el contrato.

## Archivos modificados y motivo

- `apps/staff-panel/src/App.tsx`: nav primaria `grid-cols-3` → `grid-cols-2`; eliminado el botón primario Cocina (se conserva el acceso secundario en el dropdown de Más como `Cocina (KDS)`, la ruta directa `/kitchen`/query param, la vista `KitchenOrdersManager`, sus controles E14 y el banner de llamados). También se retiró una línea blanca EOF que hacía fallar `git diff --check`. Motivo: alinear con `02-CONTRATO-SERVICIO.md` §2 (Servicio + Más primarias; Cocina configurable).
- `packages/api/test/s07-service-shell.test.ts`: la comprobación estática ahora recorta el dropdown de Más (`role="menuitem"`) y sólo exige ausencia de Cocina/Caja en la primaria; permite explícitamente `selectTab('kitchen')`, `/kitchen` y `KitchenOrdersManager` como acceso secundario/directo. No oculta texto ni cambia el contrato.
- `packages/api/test/polling-clients-reconnect.test.ts`: Bloque 5 y test final corregidos — `useSSE.ts` se comprueba como adaptador (`useServiceSync`, sin `EventSource`, sin scheduling propio); `useServiceSync.ts` demuestra importación/instanciación y uso de `PollingCoordinator` (`start`, `stop`, `triggerNow`). Sin segundo coordinador ni scheduling redundante.
- `packages/api/test/e10-table-detail-actions.test.ts`: 4 etiquetas aleatorias reemplazadas por `Mesa E10-${randomUUID().slice(0,8)}` / `Mesa S15-E10-…` usando el `randomUUID` ya importado. Sin cambios de negocio.
- `packages/api/test/capabilities-unit.test.ts`: fixture `baseConfig` con `allowWaitersToCollectCash: false`; conteo `11` → `12`. La capacidad queda en el producto (E12).
- `scripts/check-route-matrix.mjs`: `classify()` reconoce `verifySettlementAuthorization` como `STAFF` mínimo.
- `scripts/route-matrix.json`: settle y settle-and-close `MANAGER` → `STAFF` con notas (mozo con `WAITER_CASH` + flag o manager; nunca anónimo); agregadas `POST /v1/staff/restaurants/:id/service-tasks/act` (`STAFF`, E14 + tenant) y `POST /v1/staff/terminal/provision` (`ANON`, PIN del encargado validado dentro de la ruta). Ningún `preHandler` debilitado.

No se tocaron otros componentes, API, middleware (salvo lectura), migraciones, shared, `.env`, secretos, despliegues ni datos reales. No se agregaron snapshots frágiles ni PASS históricos.

## Verificación local posterior de la supervisión

La corrida inicial de OpenCode terminó `exit 0` bajo Job Object, pero no
ejecutó comandos y quedó `IMPLEMENTED_NEEDS_REVIEW`. Codex reprodujo y verificó
el estado actual:

1. Baseline previo: `node scripts/test-local.mjs` → `exit 1`, 82 suites
   pasadas, 5 fallidas, 1 skip; 772 tests pasados, 6 fallidos, 3 skips.
2. Focal posterior: `node scripts/test-local.mjs test/s07-service-shell.test.ts test/polling-clients-reconnect.test.ts test/e10-table-detail-actions.test.ts test/route-matrix-guard.test.ts test/capabilities-unit.test.ts` → `exit 0`, 5/5 suites y 43/43 tests.
3. Regresión completa actual: `node scripts/test-local.mjs` → `exit 0`, 87
   suites pasadas, 1 skip explícito; 778 tests pasados, 3 skips explícitos.
   Duración observada: 149,78 s.
4. Build: `node scripts/build.mjs` → `exit 0`, 6/6 workspaces compilados:
   shared, api, client-web, staff-panel, admin-dashboard y qr-generator.
5. Matriz: `node scripts/check-route-matrix.mjs` → `exit 0`, 107 rutas
   clasificadas, sin novedades ni deriva.
6. Higiene: `git diff --check` global → `exit 0`; `git diff --check` del
   alcance E21 → `exit 0`.

El primer focal posterior quedó `exit 1` sólo por la aserción sintáctica vieja
de `useState<ActiveTab>('service')`; Codex corrigió esa expectativa para
comprobar el inicializador lazy y volvió a ejecutar la focal con `exit 0`.
Todos los comandos acotados finalizaron con Job Object vacío. Logs y hashes de
salida se conservan en
`C:/Users/rodri/Desktop/AI/Projects/_orchestration/runs/mesaya-remediacion-e21-20260916-1`.

Evidencia de lectura: `evidencia/E21/DIAGNOSTICO.md`. Evidencia ejecutada:
`evidencia/E21/VERIFICACION-CODEX-20260916.md`.

## Estado y continuidad

- Gate E21: `VERIFIED_LOCAL` técnico. No hay P0/P1 reproducible abierto en la
  regresión local actual.
- `PENDING_HUMAN` / `PENDING_CLOUD` heredados de E15–E20 se mantienen:
  PostgreSQL multi-conexión, navegador/touch/lector, equipos, dominio,
  deployment y datos reales. Ningún estado local equivale a GO de producción.
- Próxima acción exacta: habilitar E22 y ejecutar sólo su ficha de carga y
  saturación, preservando este árbol sucio y los gates externos. No publicar,
  no hacer push/merge, no migrar datos reales ni declarar producción.
