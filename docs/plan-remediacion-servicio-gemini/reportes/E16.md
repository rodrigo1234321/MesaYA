# Reporte de Ejecución: E16 — Reportes por cobro, venta y jornada

## Ficha y alcance

- **Fecha:** 2026-09-15.
- **Ficha:** E16 (S22 / H09). Dependencia local E15 cumplida.
- **SHA de entrada y HEAD de salida:** `7bcddf6bf298f6cb15da70579fb49b9ecd7d1c83` (sin commit).
- **Ejecutor inicial:** OpenCode con `opencode/muse-spark-1.3-contributor-free`.
- **Revisión y cierre:** Codex, con diff focal, prueba adicional y gates locales.
- **Árbol:** ya estaba sucio por E00–E15; se preservaron esos cambios. No hubo
  `reset`, `clean`, `checkout`, `switch`, `merge`, `rebase`, push, deploy ni
  uso de datos reales.

## Resultado

Se unificó la semántica de `consumo`, `cobrado neto`, `propina`,
`devolución`, `saldo` y `turno` entre servicio, rutas, CSV, PDF, tipos y Admin.
La fecha original de la tanda se conserva; los cobros se imputan por su propia
fecha; cada ajuste se imputa por la fecha propia del ajuste; los turnos pueden
cruzar medianoche; los rangos son `[desde,hasta)` en la zona IANA del local.

Durante la revisión se corrigió además un borde no cubierto por la primera
prueba: una devolución sobre un settlement histórico ahora aparece como salida
negativa del período, sin volver a contar el cobro original.

## Archivos propios de E16

- `packages/shared/src/sales-types.ts`: filtros y DTOs de turno, devolución y
  fecha original.
- `packages/api/src/services/sales-reports.service.ts`: rango por turno,
  validaciones seguras, ajustes por fecha propia, cobros históricos ajustados,
  detalle con semántica de período y saldo de vida.
- `packages/api/src/routes/sales.routes.ts`: `shiftId` y filtro fiscal en
  summary/operations/CSV/PDF; etiquetas canónicas en CSV.
- `packages/api/src/services/receipt.service.ts`: bloque de turno y columna
  explícita de devoluciones en el PDF A4.
- `apps/admin-dashboard/src/lib/api.ts` y
  `apps/admin-dashboard/src/components/SalesManager.tsx`: selector de turno
  actual, criterio visible, devoluciones y error sin falso cero.
- `packages/api/test/e16-sales-reports-reconciliation.test.ts`: 10 tests S22,
  incluido settlement histórico ajustado y contrato PDF.
- `reportes/E16.md`, `evidencia/E16/**`, `CONTROL.md` y `CONTINUAR.md`.

No se modificaron `metrics.service.ts`, `metrics.routes.ts` ni `MetricsView.tsx`:
la métrica operativa existente no era necesaria para la semántica financiera.

## Verificación local

Todas las corridas usaron `scripts/test-local.mjs` o `scripts/build.mjs` con
SQLite efímera propia, sin tocar servicios ni la base demo compartida. Las
evidencias completas quedan en
`C:\Users\rodri\Desktop\AI\Projects\_orchestration\runs\mesaya-remediacion-e16-codex-20260915-1`.

| Gate | Resultado |
|---|---|
| Focal E16 `test/e16-sales-reports-reconciliation.test.ts` | exit 0, 10/10 |
| Regresión ventas/monetaria/E13/E14/E15 + E16 | exit 0, 6 archivos, 62/62 |
| `node scripts/build.mjs` | exit 0, 6/6 workspaces |
| Job Object del supervisor | vacío al finalizar cada corrida |
| `check-route-matrix.mjs` | exit 1 por 4 residuos ya conocidos de E14, ninguno creado por E16 |
| `git diff --check` | exit 2 por línea blanca EOF preexistente en `apps/staff-panel/src/App.tsx`, fuera de E16 |

S22 cubre pedido día 1/pago día 2, turno cruzando medianoche, devolución
posterior, devolución de cobro histórico, filtros, oráculo SQL/aritmético,
detalle, CSV, PDF, rango inválido y aislamiento de turno ajeno.

## Gate y pendientes

- **E16:** `VERIFIED_LOCAL`.
- **PENDING_CLOUD heredado:** S21 de E15 en PostgreSQL multi-conexión (mínimo
  20 concurrentes, mismas/distintas claves, dos locales, migración, reinicio y
  PDF persistido). No se inventa evidencia cloud.
- **PENDING_HUMAN:** revisión del Admin en navegador/tablet real y conciliación
  con el dueño; no es un motivo para rebajar el gate técnico local.
- Los cuatro hallazgos de `check-route-matrix` y la línea EOF pertenecen a
  fichas/residuos previos; quedan trazados y no se corrigen dentro de E16.

## Decisión

E16 queda cerrada localmente y habilita la coordinación de E17. No se declara
GO de producción ni certificación cloud/humana. La próxima etapa autorizada es
E17; conservar los pendientes externos y el árbol sucio existente.
