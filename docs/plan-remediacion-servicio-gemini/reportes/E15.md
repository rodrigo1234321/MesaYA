# Reporte de Ejecución: E15 — Recibos atómicos y recuperables

## Ficha y alcance autorizado

- **Ficha:** E15 (S21 / H02). Dependencias: E03/E13.
- **Objetivo:** unicidad e idempotencia de tickets bajo concurrencia real:
  contador atómico por local/período inicializado sobre números existentes,
  número final ligado a snapshot+hash+PDF persistido, cobro confirmado que no
  se revierte si falla el render.
- **Archivos autorizados tocados:** `packages/api/src/services/receipt.service.ts`,
  `packages/api/prisma/schema.prisma`, `packages/api/prisma/schema.supabase.prisma`,
  migración nueva `packages/api/prisma/migrations-postgres/20260915220000_e15_receipt_counter/**`,
  test nuevo `packages/api/test/e15-receipts-atomic-recoverable.test.ts`.
  No se tocó E00–E14 ni E16+. No se amplió alcance a otros archivos.
- **Ejecutor de la corrida:** OpenCode con `opencode/muse-spark-1.3-contributor-free`
  (sin cambio de modelo, sin otros agentes).

## SHA de entrada/salida y estado del árbol

- **SHA base de entrada:** `7bcddf6bf298f6cb15da70579fb49b9ecd7d1c83`.
- **SHA de salida:** `7bcddf6bf298f6cb15da70579fb49b9ecd7d1c83` (el trabajo se
  conserva sin commit, igual que la entrada).
- **Árbol sucio explícito:** se preservan los cambios previos E00–E14 sin
  commit (no se hizo `reset/clean/checkout/switch/merge/rebase`, no se borró
  ni revirtió nada ajeno). Cambios propios de esta corrida:
  1. `packages/api/src/services/receipt.service.ts` — contador atómico,
     emisión en escritura única, errores 503 explícitos, fin del fallback
     silencioso (`console.warn` eliminado).
  2. `packages/api/prisma/schema.prisma` y `schema.supabase.prisma` — modelo
     `ReceiptCounter` + relación en `Restaurant` (contenido idéntico salvo
     datasource, apto para `check:supabase-schema`).
  3. `packages/api/prisma/migrations-postgres/20260915220000_e15_receipt_counter/migration.sql`
     — tabla/índices/FK con `IF NOT EXISTS`.
  4. `packages/api/test/e15-receipts-atomic-recoverable.test.ts` — 6 tests
     focales (20 concurrentes, misma clave ×20, 2 locales, preexistentes,
     reinicio/PDF, cobro intacto).
  5. Este reporte, `evidencia/E15/DIAGNOSTICO.md`,
     `evidencia/E15/NOTA-VERIFICACION-PENDIENTE.md`, `CONTINUAR.md` y estado
     E15 en `CONTROL.md`.

## Diagnóstico

- **D1 — `count + 1` con 4 reintentos → 503 bajo concurrencia (CONFIRMADO por
  lectura, dinámica pendiente).** Dos solicitudes leen el mismo `count`,
  colisionan en `@@unique([restaurantId, receiptNumber])` y agotan los 4
  intentos → 503 `RECEIPT_PERSIST_FAILED`. El 503 era síntoma, no causa;
  coherente con H02 "no demostrado" de `01-EVIDENCIA-Y-COMPETIDORES.md`.
- **D2 — Carrera de idempotencia mezclada con colisión de número
  (CONFIRMADO por lectura).** Mismo `P2002` para ambos casos con intentos
  compartidos; el replay por clave existía pero competía con la colisión de
  número. Ahora: replay primero, número nuevo sin reutilizar, intentos
  separados y acotados (12 reserva / 10 emisión).
- **D3 — Persistencia en dos pasos + `console.warn` que ocultaba pérdida
  (CONFIRMADO por lectura, corregido).** `contentHash`/`pdfData` iban en un
  `UPDATE` posterior opcional. Ahora escritura única + errores explícitos
  (`RECEIPT_COUNTER_UNAVAILABLE`, `RECEIPT_SCHEMA_UPGRADE_REQUIRED`,
  `RECEIPT_RENDER_FAILED`, `RECEIPT_NUMBER_EXHAUSTED`, `RECEIPT_COUNTER_EXHAUSTED`).
- **Descartado:** secuencia fiscal sin huecos — el ticket es informativo;
  huecos por reintento documentados, sin reutilización de números.

## Fixture, comandos, exit codes y resultado

- **Fixture:** restaurantes, sesiones, órdenes, claves `e15-*` y settlements
  sintéticos; cada ejecución usó la SQLite efímera privada de `scripts/test-local.mjs`.
  No se usó `setup:local`, porque habría tocado `dev.db` compartida con servicios
  locales activos. Se regeneró sólo el cliente SQLite Prisma.
- **Ejecutor:** OpenCode/Muse Spark 1.3 terminó exit `0` bajo Windows Job
  Object; `verified_empty`, `ActiveProcesses=0`. No se aceptó esa salida como
  verificación funcional.
- **Verificación Codex:**
  - `node scripts/prisma-generate.mjs sqlite` — generación completada; el
    wrapper informó `125 unexpected_descendants` al cerrar, pero su log muestra
    `verified_empty` y el cliente requerido quedó generado.
  - `node scripts/test-local.mjs test/e15-receipts-atomic-recoverable.test.ts`
    — exit `0`, `1` archivo, `6/6` tests.
  - Regresión `sales-reports-settle`, `monetary-convergence`,
    `e13-contextual-settle-flow` y `e14-kitchen-second-station` — exit `0`,
    `4` archivos, `46/46` tests.
  - `node scripts/build.mjs` — exit `0`, build completo de `6/6` workspaces.
  - `node scripts/sync_supabase_schema.js --check` — exit `0`, schemas
    sincronizados.
  - Prisma `validate` sobre `schema.supabase.prisma` con URLs sintéticas
    `127.0.0.1:1` — exit `0`, schema válido; no abrió conexión.
  - `node scripts/check-route-matrix.mjs` — exit `1` por 4 rutas agregadas o
    modificadas en E14 (`settle`, `settle-and-close`, `service-tasks/act`,
    `terminal/provision`); no son cambios de E15 y quedan documentadas en la
    continuidad de E14.
  - `git diff --check` — exit `1` por una línea en blanco al EOF de
    `apps/staff-panel/src/App.tsx`, residuo preexistente fuera de E15.
- **Resultado:** la prueba local demuestra unicidad, idempotencia, aislamiento,
  convivencia con `0042`, persistencia/reimpresión del PDF y settlement intacto.
  No hay PostgreSQL/`pg_isready` local y Docker Desktop no tiene daemon activo;
  S21 multi-conexión queda `PENDING_CLOUD` y no se presenta como verificada.

## Evidencia

- `evidencia/E15/DIAGNOSTICO.md`: defecto, ventanas de carrera y decisión.
- `evidencia/E15/VERIFICACION-CODEX-20260915.md`: comandos, exit codes y
  resultados posteriores a la corrida del ejecutor.
- `evidencia/E15/NOTA-VERIFICACION-PENDIENTE.md`: gate PostgreSQL pendiente y
  condiciones exactas para cerrarlo.
- Sin capturas: ficha de servicio sin superficie UX; la evidencia dinámica es
  el test focal y sus consultas de persistencia.

## Límites de validación (no convertir en PASS)

- `PENDING_CLOUD`: S21 sobre PostgreSQL multi-conexión (≥20 concurrentes,
  misma/diferente clave, 2 locales, reinicio de proceso real) en entorno
  desechable aislado; migraciones E15 incluidas. No hay servidor PostgreSQL
  local (`psql`/`pg_isready` ausentes) y Docker Desktop no tiene daemon.
- `PENDING_HUMAN`: no aplica a esta ficha (sin UX).

## Revisión

- Autorrevisión por relectura de regiones editadas (interfaz restaurada tras
  edición, sin referencias restantes a `RECEIPT_PERSIST_FAILED`, esquemas
  espejados).
- **Revisión NO independiente** (mismo ejecutor). No se declara certificación
  cloud/producción.

## Gate

- **Estado E15:** `VERIFIED_LOCAL` para código, pruebas y build locales. S21
  PostgreSQL multi-conexión permanece `PENDING_CLOUD`; esto no certifica
  producción, cloud ni datos reales.

## Hallazgos residuales y próxima acción exacta

1. Ejecutar S21 en PostgreSQL multi-conexión con un entorno desechable aislado
   cuando haya un servidor disponible; mantener `PENDING_CLOUD` mientras falte.
2. Continuar con E16, ya habilitada por el goal explícito de coordinar todo el
   paquete; no abrir E17 hasta cerrar la evidencia local de E16.
3. Mantener separado el residual de `check-route-matrix`/`git diff --check`,
   que pertenece a E14 o a cambios previos.
