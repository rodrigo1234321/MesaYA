# Reporte de ejecución: E24 — Paquete de puesta en producción y pendientes externos

Fecha de corte local: 2026-09-16 (corridas cloud finalizadas el 2026-09-16/17 UTC)
Entrada de producto: rama `codex/servicio-remediacion`, candidato
`9e4a4a06152cc3c068c4b6ee07ba717fae649fe4`.
Release ejecutado: `163c1cc4be43afcb2f5402ac4eaaaa6698dde95a6`.
Worktree: `C:/Users/rodri/Desktop/AI/Projects/mdpmesasvivas-servicio-remediacion`

## Dictamen

**E24 queda finalizada en su alcance autorizado.** El paquete pasó CI, el
backup/restore temporal fue ejecutado y conciliado, la migración PostgreSQL fue
aplicada sin seed ni `db push`, los cuatro proyectos Vercel quedaron publicados
con el mismo SHA de release y el smoke remoto de lectura pasó.

Esto no convierte en `GO` global los gates que pertenecen a otras fichas:

- `PASS_LOCAL`: E00–E22 local/CI, paquete E24 y controles de release.
- `PASS_CLOUD`: backup/restore drill, migración auditada, mapping SHA/deployment,
  HTTPS, health, SPAs, CORS y resolución QR canónica read-only.
- `PASS_CLOUD_EPHEMERAL`: repetición E22 sobre PostgreSQL 17 efímero en dos
  jobs independientes, con fixture guardada, checks completos, carga de
  lectura y flujo mutante/conciliación; run `35171261968`.
- `PENDING_CLOUD`: repetición E22 contra staging/proveedor real aislado,
  revisión de costos/observabilidad y retención durable del backup.
- `PENDING_HUMAN`: E23 todavía requiere operadores, equipos, dos rondas y
  aprobación presencial. No se certifica producción total hasta ese gate.

La verificación cloud read-only posterior quedó registrada en
`evidencia/E24/VERIFICACION-CLOUD-READONLY-20260916.md`: confirma que Vercel
expone logs de runtime, pero no demuestra alertas/retención/SLO; la consulta de
costos no está disponible (`404 Costs not found`) y no hay secretos configurados
en un Environment de staging.

## Secuencia ejecutada

1. **CI del release:** run `35168551177` sobre `163c1cc4…`, con `build-and-test`
   y `postgres` en verde.
2. **Diagnóstico y corrección del drill:** el primer run
   `35168441849` falló de forma segura porque el destino PostgreSQL era 17.6 y
   el runner usaba `pg_dump` 16.15. Se corrigió el workflow en `163c1cc4…` para
   usar PostgreSQL 17 y verificar/instalar el cliente PGDG 17.
3. **Backup/restore:** run `35168679754`, exitoso. Copió temporalmente sólo
   `public`, restauró en PostgreSQL efímero y borró el dump al terminar.
4. **Migración:** run `35168778385`, job `migrate` exitoso. Aplicó únicamente
   `migrate deploy`; `preflight` y `backup_drill` quedaron omitidos por la
   selección explícita de operación.
5. **Publicación:** API, cliente, Staff y Admin quedaron `READY` en Vercel con
   el SHA `163c1cc4…` confirmado por `vercel ls --json`.
6. **Smoke posterior:** health, interfaces, CORS y QR canónica se probaron con
   peticiones de lectura; no se crearon pedidos, cobros ni sesiones.
7. **E22 PostgreSQL efímero:** el workflow `e22-postgres-load.yml` ejecutó
   nuevamente lectura y flujo mutante sobre PostgreSQL 17 efímero en el run
   `35171261968`; ambos jobs pasaron y los summaries se sanitizaron antes de
   subirlos como artefactos temporales.

## Evidencia del backup/restore

El run `35168679754` informó, sin conservar datos de negocio en el repositorio:

| Control | Resultado |
|---|---|
| Cliente `pg_dump` / `pg_restore` / `psql` | PostgreSQL 17.11 |
| Alcance | `public` |
| `BACKUP_RESTORE_DRILL` | `PASS` |
| `PUBLIC_SCHEMA_MATCH` | `PASS` |
| `CORE_ROW_DIGEST_MATCH` | `PASS` |
| `CORE_RELATIONS` | `PASS` |
| Tamaño temporal | 128697 bytes |
| Duración del restore | 2 s |
| SHA-256 del dump temporal | `ddf1c69658df325fb400c451281fff025bf90f42cac040d17ba5dd74b60fbc2b` |

El checksum sirve para la corrida, pero el artefacto fue eliminado por diseño.
Por eso queda demostrado el procedimiento de recuperación, no una política de
retención durable ni un RPO/RTO de producción.

## Deployments Vercel

| Proyecto | Deployment ID | Alias estable | Estado | SHA confirmado |
|---|---|---|---|---|
| `api` | `dpl_6isNXrvZaReuFKQaKPGaPA1sCXMd` | [api-mesa-ya.vercel.app](https://api-mesa-ya.vercel.app) | READY | `163c1cc4…` |
| `client-web` | `dpl_FzQeDWCkVWmN7dteFPo6VgWGczyH` | [client-web-mesa-ya.vercel.app](https://client-web-mesa-ya.vercel.app) | READY | `163c1cc4…` |
| `staff-panel` | `dpl_2hPtaXsncU6yPjU6MrHfr7kMTUkA` | [staff-panel-mesa-ya.vercel.app](https://staff-panel-mesa-ya.vercel.app) | READY | `163c1cc4…` |
| `admin-dashboard` | `dpl_5ve1SD9E9t7ohzMY3553xAr7ZW6k` | [admin-dashboard-mesa-ya.vercel.app](https://admin-dashboard-mesa-ya.vercel.app) | READY | `163c1cc4…` |

Vercel confirmó Node 22.x y los Root Directory configurados para cada proyecto.
El intento inicial de invocar el CLI desde `apps/client-web` fue rechazado por
duplicar la raíz; no produjo deployment. La publicación correcta se hizo desde
el monorepo con el proyecto explícito.

## Smoke remoto posterior al deploy

- `GET /health` y `GET /v1/health` del API: HTTP 200.
- Cliente, Staff y Admin: HTTP 200 y HTML.
- Preflight CORS desde los tres aliases estables: HTTP 204, origin explícita,
  credenciales `true` y métodos esperados.
- Origin `https://example.invalid`: HTTP 404 sin `Access-Control-Allow-Origin`.
- Ruta frontend canónica
  `/r/mesaya-piloto/mesa/Mesa%201`: HTTP 200.
- Resolver API canónico `/v1/sessions/mesaya-piloto/Mesa%201`: HTTP 200,
  `valid=false`, `isActive=false`, sin token. Es el resultado correcto para una
  mesa existente sin turno/sesión activa; no se hizo una mutación.
- La fixture histórica `trattoria-del-puerto` no existe en la instancia cloud
  actual; su 404 no se clasifica como regresión.

## Seguridad y límites

- No se leyeron ni registraron valores de secretos, connection strings, JWT,
  PINs ni tokens.
- El workflow de migración recibió sólo las referencias de secretos de GitHub
  configuradas y no ejecutó seed ni `db push`.
- El backup drill usó un runner efímero, alcance `public`, comparación de
  esquema/filas/relaciones y limpieza final.
- El smoke de producción fue read-only. La carga E22 no se apuntó a producción:
  la corrida cloud aprobada usó PostgreSQL efímero en GitHub, destino loopback,
  fixture dedicada y credenciales de prueba no productivas.
- E23 no se infiere desde builds, HTTP ni browser smoke; queda pendiente la
  observación humana presencial.

## Mejoras relevantes entregadas por el plan

- Operación de mozo en terminal compartida: identidad, cambio de operador,
  cola estable, avisos persistentes y soporte para tablet vertical/mouse.
- Servicio completo de mesa: estados de atención, cocina en segunda estación,
  comanda/impresión delimitada y entrega/reversión segura.
- Integridad financiera: cobros atómicos, permisos contextuales, recibos
  recuperables, reportes por venta/cobro/jornada y conciliación.
- Cliente y QR: carrito resistente al doble toque, idempotencia, sesiones
  aisladas, URL canónica `/r/<slug>/mesa/<label>` y filtros/información
  alimentaria explícitos.
- Seguridad multi-tenant: PIN hash/rotación, límites de abuso, roles, RLS y
  CORS explícito; errores sanitizados y manifiesto de instancia fail-closed.
- Confiabilidad: coordinación de polling/sync, estados offline/error, suite E21,
  perfil E22 fail-closed y paridad SQLite/PostgreSQL.
- Release cloud: migraciones auditadas, preflight, backup/restore verificable,
  trazabilidad SHA→deployment y smoke HTTPS/CORS/QR.

## Próximo gate, fuera de E24

1. Repetir E22 en staging/proveedor real aislado con credenciales y mesas de
   prueba, registrando tarifa, límites y observabilidad.
2. Completar E23 con operadores/equipos reales, fallback en papel y mediciones.
3. Definir retención durable de backups, RPO/RTO, costos y observabilidad antes
   de declarar un GO operativo definitivo.
