# Reporte de ejecución: E24 — Paquete de puesta en producción y pendientes externos

Fecha: 2026-09-16
Entrada: rama `codex/servicio-remediacion`, HEAD
`7bcddf6bf298f6cb15da70579fb49b9ecd7d1c83`, árbol sucio E00–E22 preservado.
Salida: paquete documental E24 y evidencia de relevamiento; sin commit de
release y sin mutaciones externas.
Worktree: `C:/Users/rodri/Desktop/AI/Projects/mdpmesasvivas-servicio-remediacion`

## Estado y alcance

Estado: **VERIFIED_LOCAL** para la preparación documental y los controles locales
del paquete. No es `G-HUMANO`, no es `G-CLOUD` y no es autorización de producción.

E24 se habilitó sólo en su modalidad permitida de preparación con gates pendientes:
E23 todavía no se ejecutó porque requiere personas y equipos reales. Se preservan
los cambios E00–E22 y se agregan únicamente reporte/evidencia de E24.

OpenCode/Muse Spark fue intentado como ejecutor bajo el supervisor Windows Job de
la jornada, pero el guard de memoria terminó la sesión antes de producir trabajo
útil. No se hizo fallback a una ejecución OpenCode sin aislamiento; Codex preparó
esta documentación y verificó sus controles. No se modificó código de producto.

## Diagnóstico

Confirmado:

- El candidato local no tiene SHA de salida porque conserva cambios sin commit.
- La cuenta Vercel tiene cuatro proyectos reales bajo `mesa-ya`: `api`,
  `client-web`, `staff-panel` y `admin-dashboard`.
- Los deployments Ready visibles son de aproximadamente 5–6 días atrás y no
  tienen aquí una relación demostrada con el HEAD ni con E00–E22.
- El smoke HTTP de esos aliases productivos respondió 200 en health y las tres
  SPAs; los preflight CORS de las tres origins respondieron 204 con origin
  explícita, mientras una origin no autorizada respondió 404. Es baseline de
  disponibilidad/configuración, no prueba del candidato.
- Los documentos históricos contienen nombres de proyecto incompatibles entre sí;
  el inventario remoto de lectura es la referencia para la próxima acción cloud.
- El árbol contiene 26 migraciones PostgreSQL versionadas, mientras documentos
  anteriores hablan de 23; el conteo actual debe ser el que se revise en staging.
- La configuración remota se pudo enumerar por nombre; no se descargaron ni
  guardaron valores completos. La presencia no equivale a corrección.
- El canal GitHub tiene activos `ci` y `release-migrate`. El segundo es manual,
  usa el Environment `production` y sólo inyecta
  `MESAYA_PG_DATABASE_URL` y `MESAYA_PG_DIRECT_URL` para ejecutar el script
  auditado de `migrate deploy`, sin seed ni `db push`.
- El Environment GitHub `Production` existe, pero la consulta devolvió cero
  reglas de protección. `gh secret list` no devolvió nombres para el repositorio
  ni para ese environment y tampoco devolvió variables de repositorio. Esto
  puede ser ausencia o falta de visibilidad de la configuración; en cualquiera
  de los dos casos no hay evidencia suficiente para disparar una migración.
- La consulta REST de secrets del Environment, realizada con una identidad que
  tiene permisos administrativos sobre el repositorio, devolvió
  `total_count=0`; por tanto el bloqueo de GitHub es ausencia de bindings
  configurados/visibles, no una limitación de enumeración. Vercel, en cambio,
  muestra cuatro variables `Secret` ocultas en producción (`DATABASE_URL`,
  `DIRECT_URL`, `JWT_SECRET` y `ENCRYPTION_SECRET_KEY`) que no se pueden
  transferir en claro a GitHub.
- Un intento de `vercel env run` en producción informó que un valor sensible no
  podía ser descargado. La orden no se usó para consultar PostgreSQL: no hubo
  query ni mutación remota y el generador que continuó trabajó con el entorno
  local, no con una conexión de producción demostrada.
- Revalidación posterior del canal: `release-migrate` continúa sin ejecuciones,
  `Production` continúa sin reglas de protección observadas ni nombres de
  secretos visibles, y los metadatos JSON de los cuatro deployments Ready no
  incluyen un `gitCommitSha`; el mapping candidato → deployment sigue sin
  demostrarse.
- S30 no puede correrse en este host: no están disponibles Supabase CLI ni
  `psql`, `pg_dump` o `pg_restore`, y no hay una base PostgreSQL autorizada.

No confirmado:

- commit/SHA fuente y artefacto que sirven cada URL productiva; los deployment
  IDs visibles quedaron registrados, pero no están vinculados a este candidato;
- proyecto Supabase, permisos, RLS/Data API, drift, migraciones aplicadas,
  backup restaurable, checksum o RPO/RTO;
- binding seguro y visible de los dos secretos requeridos por
  `release-migrate`, aprobadores/protecciones del Environment y un SHA de
  release que exista en GitHub;
- QR real, bundles sin secretos, costos/plan y observabilidad del candidato;
- aprobación del local, responsables y recorrido humano E23.

## Archivos agregados

- `evidencia/E24/PAQUETE-RELEASE-20260916.md`: identidad del candidato,
  inventario, procedimiento condicionado, rollback y gates.
- `evidencia/E24/DIAGNOSTICO.md`: clasificación de lo confirmado y faltante.
- `evidencia/E24/VERIFICACION-CODEX-20260916.md`: comandos, resultados y
  límites de la verificación local/remota.
- `evidencia/E24/DESBLOQUEO-CLOUD.md`: pasos seguros para configurar el canal
  remoto y reunir la evidencia de backup/restore sin Docker ni secretos en el
  chat.
- `CONTROL.md` y `CONTINUAR.md`: estado E24 y próxima acción exacta.

## Verificación local adicional

- `node scripts/build.mjs` → **exit 0**, seis de seis workspaces compilados en
  aproximadamente 52 s. Vite informó dos warnings no bloqueantes de anotaciones
  `@__PURE__` de Zod que Rollup remueve; no hubo error de compilación.
- `node scripts/build-pg.mjs`, bajo Job Object → **exit 0**; cliente Prisma
  PostgreSQL, shared y API se generaron/compilaron sin conectarse a una base.
- `node scripts/prisma-generate.mjs sqlite` → **exit 0** en una invocación
  corta posterior, restaurando el cliente de desarrollo tras la comprobación PG.
  Una envoltura supervisada previa terminó con `exit 125` por descendientes
  inesperados aunque su proceso raíz había salido `0`; se conserva como evento
  de supervisión, no como fallo del generador.
- `npm run test:local` → **exit 0** en una corrida fresca: 88 archivos de
  prueba pasaron, 1 quedó omitido; 796 tests pasaron, 3 quedaron omitidos;
  duración 134,21 s. El runner creó SQLite efímera, cargó fixtures locales y
  dejó vacío su sandbox al terminar; no quedaron procesos del proyecto.
- `npm run check:routes` → **exit 0**: 107 rutas clasificadas, sin novedades ni
  deriva.
- `npm run instance:test` → **exit 0**: 5/5 invariantes del manifiesto, sin
  secretos embebidos y con requisitos de HTTPS/aislamiento local respetados.
- `node scripts/check-load-profile.mjs` → **exit 0**: perfil E22 fail-closed,
  QR canónica, flujo opt-in y umbrales presentes.
- `npm audit --omit=dev --audit-level=high` → **exit 0**: 0 vulnerabilidades
  reportadas en dependencias de producción.
- Smoke HTTP de aliases productivos, repetido en esta continuación → **exit 0**:
  health y SPAs 200; CORS permitido 204 con origin explícita y credenciales para
  `client-web`, y 404 sin CORS para origin no autorizada. No se ejecutó ningún
  método mutante ni autenticación.

## Gate

`PASS_LOCAL` queda limitado al paquete documental y a sus verificaciones estáticas.
`G-CLOUD` permanece `PENDING_CLOUD` hasta contar con staging PostgreSQL, backup/
restore, deployment del SHA candidato, HTTPS/CORS/QR, carga y costos verificados.
`G-HUMANO` permanece `PENDING_HUMAN` hasta ejecutar E23 con operadores/equipos.
El usuario autorizó en esta sesión avanzar hacia producción, pero esa autorización
no reemplaza el backup, el canal seguro de secretos ni la trazabilidad del SHA.
El dictamen operativo actual continúa siendo **NO-GO** hasta resolverlos.

## Próxima acción exacta

La acción bloqueante es configurar por un canal seguro los secretos del
Environment `Production` (o un mecanismo remoto equivalente) y obtener backup/
restore verificable del destino. Después se debe separar y revisar el candidato,
crear un SHA limpio, ejecutar el workflow manual de migración y desplegar ese
mismo SHA en los cuatro proyectos Vercel; recién entonces corresponde repetir
E22 sobre PostgreSQL, hacer el smoke de producción con datos de prueba y evaluar
E23. No se deben pegar secretos en el chat ni usar el árbol sucio como release.
