# Reporte de ejecución: E22 — Carga funcional y saturación del puesto

Fecha: 2026-09-16
Entrada: rama `codex/servicio-remediacion`, HEAD
`7bcddf6bf298f6cb15da70579fb49b9ecd7d1c83`, con árbol sucio E00–E21 preservado
(sin `reset/clean/checkout/switch/merge/rebase`, sin borrar ni revertir
ediciones ajenas). No se detuvieron ni reiniciaron servicios existentes.
Salida: mismo SHA base, sin commit. Sólo archivos autorizados por el handoff E22.
Worktree: `C:/Users/rodri/Desktop/AI/Projects/mdpmesasvivas-servicio-remediacion`
Ejecutor: OpenCode con `opencode/muse-spark-1.3-contributor-free`. Sin cambio
de modelo, sin otros agentes.
Revisión de implementación: OpenCode ejecutó la implementación y una segunda
corrección acotada; Codex hizo revisión independiente y supervisó las pruebas.
La primera revisión corrigió aceptación incorrecta de 404/contrato de sesión
y flujo mutante sin iteraciones acotadas. La auditoría de lectura posterior de
OpenCode encontró doble conteo de errores de menú y una frontera débil del
checker frente a `catch` en preflight; ambos quedaron corregidos y verificados.
Gate: `VERIFIED_LOCAL` para la implementación, verificaciones y corridas k6
contra la fixture local aislada, más `PASS_CLOUD_EPHEMERAL` para la repetición
PostgreSQL 17 en CI. La repetición contra staging/proveedor real queda
`PENDING_CLOUD` y la observación con mozos/equipos queda `PENDING_HUMAN`; este
gate no certifica capacidad de producción ni un número de mesas.

## Alcance y diagnóstico

E22 corrige los falsos PASS del perfil anterior, confirmados por lectura
(detalle en `evidencia/E22/DIAGNOSTICO.md`): defaults de URL/PIN, logins con
`catch` vacío que degradan a `null`, fallback a `/health` cuando falta token,
`200 or 404` en menú/sesión, QR fijo a `/1` sin `valid/token/pertenencia`,
sin recorrido de negocio, sin idempotencia ni conciliación y sólo umbrales
genéricos de k6.

Hipótesis descartadas: ninguna; los siete falsos PASS se confirmaron en el
código previo antes de reescribir.

Durante la revisión independiente se confirmó que el perfil final no acepta
404 como éxito en QR/sesión, exige la forma real de `GET
/v1/orders/session/:token`, y parametriza el flujo mutante con
`K6_FLOW_ITERATIONS` mediante `per-vu-iterations`, manteniendo el límite
fail-closed de VUs/duración.

Los umbrales p50/p95/p99 y de error quedan explicitados como metas de prueba
revisables antes del ensayo. No se inventó una aprobación humana: cualquier
cambio debe quedar registrado junto al `summary-export` del operador.

## Archivos modificados y motivo

- `tests/load/pilot-profile.js` (reescrito, único perfil canónico):
  fail-closed total. `setup()` exige `API_URL`, `RESTAURANT_SLUG`,
  `WAITER_PIN`, `MANAGER_PIN` (sin defaults, sin imprimir secretos);
  preflight con `/v1/health` sólo como conectividad, login mozo+manager 200
  con mismo `restaurantId` (aborta ante status/JSON/excepción, sin
  `try/catch`→null); menú con `restaurant/categories/item disponible
  id/precio>0`; mesas con staff e `id/label/currentState`; listas explícitas
  `K6_GUEST_TABLE_LABELS`/`K6_FLOW_TABLE_LABELS` (aborta si falta la necesaria,
  sin asumir `Mesa 1`); QR canónica por mesa lectora (`200`, `valid===true`,
  token, pertenencia restaurante/mesa; nunca 404 ni legacy); workspace,
  `kitchen-orders`, `cash-orders` y `sales/summary` validados una vez en
  preflight. Lectores con semántica (no sólo status), tags de polling y
  métrica `polling_requests`. Escenarios parametrizados
  (`guest_readers/salon_poll/kitchen_poll/cash_poll` + opt-in
  `business_flow`), VUs visibles, techo `30` VUs / `300` s (aborta si se
  supera), duración registrada; VUs cero = escenario omitido. Métricas
  `business_check_pass`, `business_error_rate`, `business_latency_ms`,
  `polling_requests`, `business_flow_completed`, `business_reconciliation_ok`;
  umbrales `p(50)<500/p(95)<800/p(99)<1500`, `business_check_pass>0.99`,
  `business_error_rate<0.01`, `checks>0.99`, `http_req_failed<0.01`, y con
  flujo `count>0` en finalización y conciliación (metas de prueba, no
  capacidad certificada). Flujo `business_flow` sólo con
  `K6_BUSINESS_FLOW=true`: mesas de flujo `AVAILABLE` y suficientes para sus
  VUs, disjuntas de lectoras; pasos crear→preparar(+replay)→entregar(+replay)
  →cuenta coherente→`settle-and-close` con manager (`WAITER_CASH`,
  `amountMinor` = saldo, `expectedAccountVersion`, replay sin duplicar)
  →`state/tap skip_to AVAILABLE` con `TO_CLEAN` esperado→conciliación
  (sesión fuera de caja/cocina/workspace + mesa `AVAILABLE`; métrica sólo si
  todo coherente). Fallo en un paso = check fallido + `return` sin estados
  inventados, sin reintento ciego; claves únicas por corrida/VU/iteración.
- `tests/load/README.md` (nuevo): variables obligatorias/opcionales,
  separación lectoras/mutantes, comandos con `--summary-export`,
  límites/techo, perfil exacto y lectura de p50/p95/p99, errores, checks,
  polling, conciliación y fórmula explícita para requests/costo supuesto,
  separando espera humana. `API_URL` sólo staging/local aislado; la carga
  mutante cambia datos.
- `scripts/check-load-profile.mjs` (nuevo, sin k6): inspecciona el perfil y
  falla ante defaults PIN/URL, fallback a health, aceptación de 404, falta de
  métricas/umbrales/flujo/preflight y cualquier `catch` dentro de
  `setup()`/`loginOrFail()`. Salida breve y accionable.
- `packages/api/test/e22-load-profile.test.ts` (nuevo): contrato estático
  focal (seguridad/semántica, sin carga real ni secretos).
- `docs/plan-remediacion-servicio-gemini/reportes/E22.md` (este reporte),
  `evidencia/E22/DIAGNOSTICO.md`, `CONTROL.md` y `CONTINUAR.md` (sólo estado
  honesto E22, gates previos conservados, E23 no habilitada).

Durante la implementación no se tocaron backend, middleware, migraciones,
shared, apps, `.env`, secretos, despliegues ni datos reales. La ejecución local
posterior creó sólo la fixture aislada `.tmp/e22-k6-local.db` y los archivos
`.env` locales ignorados por Git; no agregó endpoints ni datos reales. No se
instalaron dependencias del proyecto; k6 se instaló aparte por autorización
explícita. Sin `sleep` de sincronización en el flujo (sólo pacing de lectura
documentado).

## Verificación independiente de supervisión

La sesión de OpenCode no expone shell (`node`/`git`/k6), por lo que no se
tomaron sus afirmaciones como evidencia. La supervisión ejecutó bajo Job
Object, con `verified_empty` en cada corrida:

1. `node scripts/check-load-profile.mjs` → **exit 0**: perfil fail-closed,
   env exigido, QR canónica, negocio semántico, flujo opt-in y
   métricas/umbrales.
2. `node scripts/test-local.mjs test/e22-load-profile.test.ts` → **exit 0**:
   1 suite, 18/18 tests.
3. `git diff --check` → **exit 0**.
4. `node --check tests/load/pilot-profile.js` → **exit 0**.
5. `full-system-e2e` junto con el test E22, en ambos órdenes → **exit 0**:
   2 suites, 57/57 tests.
6. `full-system-e2e` aislado → **exit 0**: 39/39 tests.
7. Regresión completa, primer intento → **exit 1**: 87 suites pasaron, 1
   falló, 1 quedó omitida; 778 tests pasaron, 17 fallaron y 3 quedaron
   omitidos. Los 17 fallos se concentraron en `full-system-e2e`, con
   `findUnique where id: undefined`.
8. Regresión completa, reintento → **exit 0**: 88 suites pasaron, 1 quedó
   omitida; 795 tests pasaron y 3 quedaron omitidos.
9. Auditoría adicional de OpenCode en modo sólo lectura → **exit 0**: dos
   defectos menores concretos identificados; OpenCode corrigió sólo los tres
   archivos E22 autorizados (`pilot-profile.js`, checker y test focal).
10. Regresión completa posterior a esas correcciones → **exit 0**: 88 suites
    pasaron, 1 quedó omitida; 796 tests pasaron y 3 quedaron omitidos.

## Ejecución k6 local posterior

Se instaló k6 `v1.2.3` en el host mediante el gestor de paquetes autorizado y
se preparó una base SQLite nueva, `.tmp/e22-k6-local.db`, sin tocar la base
compartida ni ningún proveedor remoto. El API se ejecutó sólo en loopback
(`http://127.0.0.1:3000`) con el seed local de prueba. Las corridas no
registran PINs, tokens ni headers.

### Lectura y polling

Summary: `evidencia/E22/e22-local-read-summary-20260916-v2.json`.

- Duración 95 s; escenarios `guest_readers`, `salon_poll`, `kitchen_poll` y
  `cash_poll`; máximo 8 VUs; 239 iteraciones.
- `http_reqs=463`, `polling_requests=450`, `non_polling_requests=13`.
- `business_check_pass=450/450`, `business_error_rate=0/450`, checks
  `546/546` y `http_req_failed=0/463`.
- `business_latency_ms`: p50 `18.2197 ms`, p95 `46.78582 ms`, p99
  `65.805098 ms`.
- Todos los thresholds declarados pasaron.

### Flujo mutante y conciliación

Summary: `evidencia/E22/e22-local-flow-summary-20260916-v2.json`.

- Duración 95 s; cinco escenarios con `business_flow` opt-in; máximo 9 VUs;
  240 iteraciones.
- `http_reqs=477`, `polling_requests=450`, `non_polling_requests=27`.
- `business_check_pass=461/461`, `business_error_rate=0/461`, checks
  `557/557` y `http_req_failed=0/477`.
- `business_latency_ms`: p50 `12.301 ms`, p95 `37.804555 ms`, p99
  `124.608851 ms`.
- `business_flow_completed=1` y `business_reconciliation_ok=1`. El flujo
  validó crear pedido, preparar y replay, entregar y replay, cuenta, cobro y
  cierre y replay, mesa `AVAILABLE` y conciliación final.

Ambas corridas usaron el flag
`--summary-trend-stats=avg,min,med,max,p(50),p(90),p(95),p(99)`, por lo que los
percentiles anteriores son observados y no sólo thresholds declarados. El
costo no se infiere: `non_polling_requests` queda como conteo y cualquier
precio requiere una tarifa vigente del destino. Antes de conservar los
artefactos se reemplazó `setup_data`, que k6 había exportado con tokens de
sesión, por una marca `redacted`; las métricas y grupos de checks se mantienen.

El pase aislado y las dos pruebas en orden invertido, seguido de dos pases
completos posteriores (uno antes y otro después de la corrección), clasifican
el primer resultado como interferencia o flakiness del lote compartido, no
como regresión introducida por E22. No se modificó `full-system-e2e` para
esconder el incidente; queda trazado para una mejora posterior de aislamiento
si vuelve a reproducirse. El doble conteo de menú ya no existe y el checker
protege la frontera de preflight.

El autocontrol estático de OpenCode quedó además corroborado por checker,
test focal y lectura independiente: no hay defaults
`localhost:3000`/`1234`/`9999`, aceptación `200 or 404`, health como sustituto,
ni QR fija; sí hay preflight de negocio, flujo opt-in,
`ORDER_PREPARATION/ORDER_DELIVERY`, `settle-and-close`, `state/tap`, replay,
conciliación y métricas/umbrales.

## Estado y continuidad

- La carga funcional local y la conciliación ya fueron ejecutadas en un destino
  aislado reproducible y pasan los umbrales declarados; por eso E22 queda
  `VERIFIED_LOCAL`. S28 tiene evidencia de preflight, carga semántica y
  conciliación en los dos summaries indicados.
- La repetición en PostgreSQL 17 efímero de GitHub Actions quedó verificada como
  subgate `PASS_CLOUD_EPHEMERAL`: dos jobs independientes, fixture guardada con
  guardia de destino local, lectura/polling y flujo mutante con conciliación.
  Esto valida la semántica reproducible del cliente PostgreSQL, pero no
  certifica un proveedor, tarifa, límites ni observabilidad de producción.
- Sigue `PENDING_CLOUD` la repetición contra staging/proveedor real aislado, la
  determinación de límites/costos/observabilidad del entorno que se quiera
  publicar y la retención durable del backup. Sigue `PENDING_HUMAN` la
  observación y el ensayo con mozos/equipos. Ningún resultado local certifica
  producción ni mesas soportadas.
- Gates `PENDING_HUMAN`/`PENDING_CLOUD` de E15–E21 se conservan. E23 no se
  habilita. No publicar, no hacer push/merge, no migrar datos reales.
- Próxima acción exacta: repetir el mismo perfil en staging/proveedor real
  aislado con variables obligatorias, mesas explícitas y `--summary-export`,
  registrar proveedor/tarifa/limitaciones sin secretos y luego preparar E23
  cuando haya personas y equipos disponibles.

## Repetición PostgreSQL efímera en CI

El workflow manual y branch-gated
`.github/workflows/e22-postgres-load.yml` quedó en el commit
`b5beba87b3de703bf0bd57159d4964928f2841f7` y pasó en el
[run 35171261968](https://github.com/rodrigo1234321/MesaYA/actions/runs/35171261968).
Cada matriz ejecutó PostgreSQL 17 efímero, `migrate deploy`, build PG, seed
con `E22_PG_EPHEMERAL=true` y guardia estricta contra hosts remotos. El summary
se sanitizó antes de subirlo como artefacto temporal de siete días, con
`setup_data` reemplazado por `redacted`.

| Job | Resultado observado |
|---|---|
| `k6-read` | `463` requests, `450` polling, checks `546/546`, business pass `1`, error `0`, p95 `27.513324 ms`, `http_req_failed=0/463` |
| `k6-flow` | `477` requests, `450` polling, checks `557/557`, business pass `1`, error `0`, p95 `24.301224 ms`, flujo `1`, conciliación `1`, `http_req_failed=0/477` |

La primera ejecución del workflow (`35170807494`) fue corregida de forma
acotada: el wrapper TypeScript usaba `await` de nivel superior bajo CommonJS y
el sanitizer esperaba una forma vieja del JSON de k6. La corrida final ya
incluye ambos arreglos y terminó con los dos jobs en verde. Las advertencias de
deprecación de Node 20 corresponden a las actions de GitHub y no afectaron el
resultado.
