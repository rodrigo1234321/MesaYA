# Reporte E00 — Baseline y Reproducción de Diagnóstico

**Fecha:** 14 de Septiembre de 2026  
**Ejecutor:** Antigravity (Gemini 3.8 Flash High)  
**Rama:** `codex/servicio-remediacion`  
**Worktree:** `C:/Users/rodri/Desktop/AI/Projects/mdpmesasvivas-servicio-remediacion`  
**SHA de Entrada:** `7bcddf6bf298f6cb15da70579fb49b9ecd7d1c83`  
**SHA de Salida:** `7bcddf6bf298f6cb15da70579fb49b9ecd7d1c83` (baseline congelado sin cambios de producto)  

---

## 1. Verificación del Entorno y Worktree

- Se creó el worktree limpio `mdpmesasvivas-servicio-remediacion` sobre la rama `codex/servicio-remediacion` apuntando a `7bcddf6`.
- Se enlazaron las dependencias existentes mediante junctions de Windows sin alterar el checkout original `mdpmesasvivas` ni el de auditoría `mdpmesasvivas-audit-7bcddf6`.
- Verificación de herramientas de compilación:
  - `node scripts/check-route-matrix.mjs` -> **105 rutas clasificadas OK (exit 0)**.
  - `npm run build:shared` -> **TypeScript compiler exit 0**.

---

## 2. Clasificación y Reproducción de Hallazgos Base

| ID | Hallazgo | Estado de Reproducción | Evidencia en Código |
|---|---|:---:|---|
| **H01** | Columna `pinFingerprint` ausente en PostgreSQL | **CONFIRMADO ESTÁTICO** | `packages/api/prisma/schema.prisma:589,595` tiene el campo y unique index, pero `packages/api/prisma/migrations-postgres/` contiene 23 migraciones y **cero** menciones a `pinFingerprint`. |
| **H02** | Colisión concurrente en numeración de tickets | **CONFIRMADO ESTÁTICO** | `receipt.service.ts:37-46` usa `count + 1`. Si dos transacciones concurren, ambas calculan el mismo correlativo y colisionan en `@@unique([restaurantId, receiptNumber])`. |
| **H03** | Rotación de secreto criptográfico bloquea al personal | **CONFIRMADO ESTÁTICO** | `staff.service.ts:11-14,35-50` busca por huella y sólo hace fallback a `pinFingerprint: null`. Una huella firmada con una clave previa queda bloqueada. |
| **H04** | Rate limit penaliza logins legítimos en terminal compartido | **CONFIRMADO ESTÁTICO** | `staff.routes.ts:47-56` descuenta del bucket (5 en 5 min) **antes** de verificar el PIN. 6 logins válidos bloquean la pantalla con HTTP 429. |
| **H05** | Mozo de salón no puede cobrar sin Manager | **CONFIRMADO ESTÁTICO** | `ServiceWorkspace.tsx:563-572` intercepta el botón de cobro y abre modal de reautorización para cualquier rol distinto de `MANAGER`. `order.service.ts:924` retorna 403. |
| **H06** | Cocina sin monitor KDS ni comandas | **CONFIRMADO ESTÁTICO** | `KitchenOrdersManager.tsx` no está montado en las rutas de `staff-panel`. No hay integración de impresión directa. |
| **H07** | Indicador de conexión engañoso y pérdida de alertas | **CONFIRMADO ESTÁTICO** | `App.tsx:65,95-98` usa `useSSE(..., false)` (calls siempre vacíos) y pone punto verde si `activeTab === 'service'` aunque no haya red. |
| **H08** | Doble toque duplica platos en cliente web | **CONFIRMADO ESTÁTICO** | `apps/client-web/app.js:1479-1554` no desactiva el botón durante la petición HTTP `POST /orders/items`. |
| **H09** | Desfase temporal en reportes de medianoche | **CONFIRMADO ESTÁTICO** | `sales-reports.service.ts:207-225` filtra órdenes por `createdAt` en vez de fecha de servicio/turno. |
| **H10** | Token temporal con alcance de Manager general | **CONFIRMADO ESTÁTICO** | `auth.middleware.ts` no valida el claim `temp: true`, permitiendo acceso a endpoints de configuración durante 300s. |

---

## 3. Estado de la Ficha E00

- **Dictamen:** **VERIFIED_LOCAL**
- **Siguiente Acción:** Proceder a **E01** (Contrato de tareas y permisos de salón).
